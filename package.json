{
  "name": "@ekubo/sdk",
  "version": "0.0.9-alpha.0",
  "private": false,
  "description": "Shared math and protocol encoding utilities for Ekubo",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EkuboProtocol/typescript-sdk.git"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "bun test",
    "build": "bun build ./src/index.ts --outdir dist --format esm --sourcemap && tsc -p tsconfig.json",
    "prepublishOnly": "rm -rf dist && bun run build"
  },
  "files": [
    "dist"
  ],
  "author": "Ekubo Inc <eng@ekubo.org>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "packageManager": "bun@1.3.3",
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "typescript": "^5.7.3"
  }
}
